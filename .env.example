# Copy this to .env and adjust as needed.
# esync reads these via clap's env support.

ESYNC_CONFIG=esync.yaml
ESYNC_LOG=debug

# Override individual connection strings if you don't want them in esync.yaml
# DATABASE_URL=postgres://esync:esync@localhost:5432/esync
# ES_URL=http://localhost:9200
